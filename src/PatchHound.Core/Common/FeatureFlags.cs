namespace PatchHound.Core.Common;

public static class FeatureFlags
{
    public const string Workflows = nameof(Workflows);
    public const string AuthenticatedScans = nameof(AuthenticatedScans);
}
